<UserControl x:Class="BnsMaterialTracker.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <ScrollViewer Background="{StaticResource BgApp}" Padding="20">
    <StackPanel>

      <TextBlock x:Name="TxtTitle" Text="倉庫管理"
                 FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

      <!-- 金幣 -->
      <Border Style="{StaticResource Card}">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="160"/>
          </Grid.ColumnDefinitions>
          <TextBlock Grid.Column="0" Text="💰" FontSize="22"
                     VerticalAlignment="Center" Margin="0,0,12,0"/>
          <TextBlock x:Name="TxtGoldLabel" Grid.Column="1" Text="金幣"
                     FontSize="15" FontWeight="SemiBold" VerticalAlignment="Center"/>
          <TextBox x:Name="TxtGold" Grid.Column="2" Height="36"
                   TextChanged="TxtGold_Changed"/>
        </Grid>
      </Border>

      <!-- 動態材料群組 (由 code-behind 填入) -->
      <ItemsControl x:Name="GroupList">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <Border Style="{StaticResource Card}">
              <StackPanel>
                <TextBlock Text="{Binding GroupLabel}"
                           FontSize="15" FontWeight="SemiBold" Margin="0,0,0,12"/>
                <ItemsControl ItemsSource="{Binding Items}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate>
                      <Grid Margin="0,4">
                        <Grid.ColumnDefinitions>
                          <ColumnDefinition Width="30"/>
                          <ColumnDefinition Width="*"/>
                          <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Icon}"
                                   FontSize="18" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding Name}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="2" Height="32"
                                 Text="{Binding Amount, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                 Tag="{Binding Id}"
                                 TextChanged="MatAmount_Changed"/>
                      </Grid>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>
              </StackPanel>
            </Border>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

    </StackPanel>
  </ScrollViewer>
</UserControl>
